apiVersion: apps/v1
kind: Deployment
metadata:
  name: cc-ui
  namespace: command-center
  labels:
    app: command-center
    component: cc-ui
    tier: UI
spec:
  replicas: 1
  selector:
    matchLabels:
      app: command-center
      component: cc-ui
      tier: UI
  template:
    metadata:
      labels:
        app: command-center
        component: cc-ui
        tier: UI
    spec:
      containers:
      - name: cc-ui
        image: 
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "0.2"
            memory: "200Mi"
          limits:
            cpu: "0.4"
            memory: "400Mi"
      imagePullSecrets:
      - name: dfdpcred
      nodeSelector:
        agentpool: agentpool
---
apiVersion: v1
kind: Service
metadata:
  name: cc-ui-service
  namespace: command-center
  labels:
    app: command-center
    component: cc-ui
    tier: UI
spec:
  selector:
    app: command-center
    component: cc-ui
    tier: UI
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
